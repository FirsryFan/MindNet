docs: 记录与 Furnace（Flutter 端）的接口对齐结果

Furnace 侧起草了 docs/MINDNET_CONTRACT.md（在它的仓库里），提出 6 个问题。
我在它的 §6 写了回应（基于 commit c624884），同时把结论固化成我这边的新增交付：

- tools/conformance.js + conformance/mindnet_vectors.json（tierA 43 条纯数学 +
  tierB 1 条快层逐轮）+ test/conformance.test.js：跨语言移植的对拍基准，
  由实现生成、--check 防过期，容差写在文件里（集合/枚举逐位相等，浮点 rel 1e-12）
- npm scripts: io / io:check / conformance
- docs/MEMORY_RECORD.md：新增 §3.5 记录这次对齐（对方要什么、我给了什么、
  关键澄清、以及待对方提供的验证数据）

回应里纠正的几处事实与澄清（都有本机实测依据）：
- src/ 已 12 文件 3942 行；src/index.js 与 mechanisms/index.js 是 Node-only（fs/require 聚合），
  其余模块双端 —— 直接决定 tierA/tierB 的移植范围
- 对方的 FSRS 曲线与 MindNet 的幂律曲线是同一个函数（γ=0.1542、c=0.980346），
  差别只在单位（天 vs 小时）与 R0/Σ/复习类型这三层 —— 移植量比想象小
- 模型不认 mastery 标量；ms 是导出量不能反复写回；grade 只影响 D 不影响同次复习的 S
- 标签树的父子边是"包含"语义，MindNet 的边是"线索"语义（严格有向）；ls 无标定来源
- DiffusionBoost 与 MindNet 扩散的七处实质差异 + 短期分工/长期取代的建议

验证：npm test 129/129；node tools/mechanisms.js --check PASS。
