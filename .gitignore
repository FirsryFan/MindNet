# 依赖（本项目零依赖，这里只是防手滑）
node_modules/

# 运行时/测试临时目录（内核约定：临时文件一律放组件内，不写系统 TEMP）
.tmp/
.tmp_verify/
.probe/
.tmp_out/
tmp_*/

# 输出产物
state.json
*.log
data/            # I/O 存档（runs.jsonl）与真实材料，不进仓库
out/             # ingest 的产出（卡片/Anki/TTS/请求）

# 系统杂项
.DS_Store
Thumbs.db
desktop.ini
